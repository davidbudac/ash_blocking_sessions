# AGENTS.md

All agent/contributor guidance for this repository lives in **[CLAUDE.md](CLAUDE.md)** —
architecture, commands, template structure, the 4-level testing guide, and
operational gotchas. Read that file; do not duplicate its content here.

(This file previously carried an out-of-date copy of that guidance — e.g. it
described the emitter as using SQL `REPLACE()`, which is exactly the approach
CLAUDE.md forbids (`ORA-22828` above 32K). Keeping one source of truth avoids
that kind of drift.)
